{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">

        <div class="alert alert-dismissable alert-success">
            <strong>
                <i class="fa fa-check"></i>
                {{ entity.nombre|title }} {{ entity.apellidos|title }} </strong> hemos recibido tu registro.<br>
            Te enviaremos un correo a la dirección que nos proporcionaste: <strong><i>{{ entity.mail }}</i></strong>
            <br>
            <p class="small">Si no recibes el correo de confirmación puedes verificar en tu carpeta de correos no deseados, si no lo has recibido puedes escribir a <strong>webmaster<i class="fa fa-at"></i>matmor.unam.mx</i></strong> para confirmar tu registro</p>
        </div>
        <div class="col-lg-7 col-lg-offset-5">
            <a href="{{ url }}" type="button"  class="btn btn-primary">Finalizar</a>
        </div>
    </div>

{% endblock %}
